/**
 * given a list of transactions for a given user verify the transaction using the following criteria
* - individual transactions are valid
* - transactions when ordered form a sequence of valid state transitions
* - the amount is consistent between individual transactions in the chain
* - fee transactions are correctly related to the right transaction if they exist
* - when ordered transactions start and end in valid states
 */

import { describe } from "vitest";

describe.skip('', () => {});