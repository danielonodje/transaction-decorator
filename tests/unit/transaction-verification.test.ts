/**
 * verifies an individual transaction
 * things like having the right fields
 * having no missing fields
 * for enumerated types, having the supported values
 * for date types, the date is valid i.e updatedAt if exists comes after the created at field
 * metadata if it exists contains the correct fields
 * 
 */
import { describe } from "vitest";

describe.skip('', () => {});